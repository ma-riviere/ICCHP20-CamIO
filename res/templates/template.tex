% This is template.tex, a sample chapter demonstrating the
% LLNCS macro package for Springer Computer Science proceedings;
% Version 2.20 of 2017/10/04

\documentclass[runningheads]{$templates$llncs}

\usepackage{graphicx}
\usepackage[utf8]{inputenc}
\usepackage[hyphens]{url}
\usepackage{hyperref}
\usepackage{etoolbox}

$preamble$

$if(highlighting-macros)$
$highlighting-macros$
$endif$

% Include packages from YAML header
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newenvironment{cslreferences}%
  {$if(csl-hanging-indent)$\setlength{\parindent}{0pt}%
  \everypar{\setlength{\hangindent}{\cslhangindent}}\ignorespaces$endif$}%
  {\par}
$endif$

$for(header-includes)$
$header-includes$
$endfor$
%
\begin{document}
%
\title{$title$ $if(thanks)$\thanks{$thanks$}$endif$}
%
$if(subtitle)$\subtitle{$subtitle$}$endif$
%
$if(titlerunning)$\titlerunning{$titlerunning$}$endif$
%
\newbool{flag}
\boolfalse{flag}
%
\author{
$for(authors)$
\ifbool{flag}{\and}{\booltrue{flag}}$authors.name$\inst{$authors.inst$}$if(authors.orcid)$\orcidID{$authors.orcid$}$endif$
$endfor$}
%
$if(authorrunning)$
\authorrunning{$authorrunning$}
$endif$
%
\boolfalse{flag}
%
\institute{
$for(institutes)$
\ifbool{flag}{\and}{\booltrue{flag}}
$if(institutes.name)$ $institutes.name$ $endif$\\
$if(institutes.emails)$ \email{$institutes.emails$} $endif$
$endfor$}
%
\maketitle
%
\begin{abstract}
$abstract$
\\
\boolfalse{flag}
\keywords{
$for(keywords)$ 
\ifbool{flag}{\and}{\booltrue{flag}}
$keywords$
$endfor$}
\end{abstract}
%
%
$body$
%
% ---- Bibliography ----
\bibliographystyle{$templates$splncs04}
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}

\end{document}
